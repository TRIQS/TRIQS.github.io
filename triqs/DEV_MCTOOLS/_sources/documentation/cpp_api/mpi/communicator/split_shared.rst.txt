..
   Generated automatically by cpp2rst

.. highlight:: c
.. role:: red
.. role:: green
.. role:: param


.. _mpi__communicator__split_shared:

mpi::communicator::split_shared
===============================

*#include <mpi/communicator.hpp>*



**Synopsis**

 .. rst-class:: cppsynopsis

       | :ref:`shared_communicator <mpi__shared_communicator>` :red:`split_shared` (int :param:`split_type` = MPI_COMM_TYPE_SHARED,
       |               int :param:`key` = 0) const











Parameters
^^^^^^^^^^

 * :param:`split_type` Type of processes to be grouped together.

 * :param:`key` Determines the rank of the process in the new communicator.


Returns
^^^^^^^

If mpi::has_env is true, return the split `MPI_Comm` object wrapped in a new mpi::communicator, otherwise
 return a default constructed mpi::communicator.

.. warning::
    This allocates a new communicator object. Make sure to call free on the returned communicator when it
 is no longer needed.